user.name
email
git clone url
cd initializing_git
echo "hello everyone" > filename(cse27.txt)
git add cse27.txt
got commit -m "initial commit"
git push origin main
vscode
new folder in pc
open in vsc
new file index.html
terminal
git init


when u clone u dont need to git init because it automatically decides the origin and tracks all the files
but when u create a new repo and folder u need to set origin and initialize git 
so u need to do git init.

4-11-2025
CSS- cascading style sheets
3 main ways to add css- inline,internal,external
1. inline- directly written inside html using style attribute
2. internal- add a style tag in head tag
3. external- css in another file, use link tag, relation attribute, href to link in to your main html file
HW- semantic asemantic, block inline, css videos 

6-11-2025

selectors in css-

1. grouping selector-
styling different elements in one go
h1,h2 { 
property: value;
}

2. element selector(make file in vscode)-

3. class selector-
to select a class, eg. class=highlight, use ".highlight" in the style.css sheet to access it and apply style

4. id selector-

5. descendent selector

11-11-2025

div- contains block level space, used to group elements
span- comtains inline level space
hover effect
(r,g,b,a)-> a is opacity/tramsparency
margin: 10px -> top right bottom left all siodes have 10px
margin: 10px 30px -> top bottom, left right
margin: 10px 30px 20px -> top , left right, bottom
margin: 10px 20px 30px 40px -> clockwise order
new css syntax -> 
margin-left: px
margin-right: px
margin-top: px
margin-bottom: px

13-11-2025

